# Contributing

1. Create a focused branch.
2. Install development dependencies with `pnpm install`.
3. Add or update a failing test before implementation changes.
4. Run `pnpm test`.
5. Keep changes scoped and do not commit secrets or generated dependency directories.

Changes to command execution, timeouts, path handling, or MCP protocol behavior must include regression tests.
